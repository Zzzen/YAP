.yap-video
    a.closeButtonParent( (click)="play()", [style.font-size.px]="preferenceService.preference.fontSize", [style.color]="preferenceService.preference.fontColor" ) {{getFilename(video.fullpath)}}
        button.btn.badge( (click)="onRemove()" ) X