#rootList( [class.isShown]="isShown && !focusSetting", [style.width.px]="preference.widthOfPlayList" )
    ul
        #handler_vertical( (mousedown)="startDragging()" )
        li( *ngFor="let x of data" )
            yap-item( [data]="x" )