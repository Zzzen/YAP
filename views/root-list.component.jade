yap-player

#rootList( [class.isShown]="isShown", [style.width.px]="width" )
    ul
        #handler_vertical( (mousedown)="startDragging()" )
        li( *ngFor="let x of data" )
            yap-item( [data]="x" )